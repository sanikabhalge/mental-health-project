error 500 : internal server error 
The request reached the backend server,
but something inside the server crashed.
problem in backend
1.issue occured during shifting the python version was due to bycrpt version issue we nees the bcrypt==4.0.1

error 402 : external api call error ,check the routers

2.For your AI therapy project with multimodal emotion detection, the current architecture using REST requests every few seconds will not scale well. It causes:
hundreds of HTTP requests
latency
server overload
difficult synchronization between audio + video

A better design is stream-based architecture using WebSockets and an emotion agent pipeline.

error in ffpmeg format you need to install add the path in your system

for every first start on different machines 
1.delete the mindcare.db